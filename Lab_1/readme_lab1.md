# Цель работы: Изучние базового HTML и СSS

## 🔧 Что такое вообще HTML и CSS:

`HTML (HyperText Markup Language)  - это язык разметки он относится в frontend, он нужен для того, чтобы сделать разметку нашего сайта
CSS (Cascading Style Sheets) - это язык для стилизации, так же относится к frontend, и служит чтобы добавить стилей к HTML файлам`

## 🛠️ Практическое задание: Создание калькулятор на HTML и CSS

📍 Ход работы и как делал калькулятор:
- ### создал HTML файл
   - В body(главная часть нашего файла) создал div с классом самого калькулятор
   - внутри класса калькулятора создал еще один div c методом input(он нужен нам для вывода инфорамции)
   - добавил еще div'ов с  кнопоками с указание их классов и подклассов.

- ### создал CSS файл
  - в самом начале я сделал стиль для body, а именно: 
    - через display сделал, чтобы можно было выравнивть контент, делаю все по центру.
    - Сделал черный задний фон
  - Приступил уже к настройке DIV класса калькулятор
    - задал ширину, отсуп, закругление через width, padding, border-radius соотвественно
  - Начал настраивать кнопки 
    - задал значение родительского класса btn:
      - margin-right, margin-top - отступы
      - width, height - ширина, высота
      - border-radius - закругление
      - background - цвет заднего фона
      - color - цвет текста 
      - font-size, font-family - размер шрифта, шрифт
      - cursor - курсор при наведение
      - user-select - нельзя выделить

  - в дочерних классах primary, secondary заменил цвета через тот же background

  - сделал изменение цвета при нажатие и наведение через методы active, hover соотвественно

  - заменил ширину в классе execute(знак = ),  чтобы он был шире отсальных кнопок

  - задал значение в классе result:
    - задал ширину
    - высоту, отступы
    - задний фон
    - шрифт
    (все методы для этого писал выше)

## ✅ Итог: Я научился создавать базовую структру HTML и CSS, создавать DIV, классы и подкласы, а также стилизации при помощи CSS и создал простой калькуятор
